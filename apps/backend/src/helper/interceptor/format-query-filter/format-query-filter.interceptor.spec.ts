import { FormatQueryFilterInterceptor } from './format-query-filter.interceptor';

describe('FormatQueryFilterInterceptor', () => {
  it('should be defined', () => {
    expect(new FormatQueryFilterInterceptor()).toBeDefined();
  });
});
